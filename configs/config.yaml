project:
  name: "Cricket DRS LBW"
  version: "0.1.0"

paths:
  data: "data"
  raw_videos: "data/raw"
  frames: "data/frames"
  annotations: "data/annotations"
  processed: "data/processed"
  experiments: "experiments"
  models: "models"

environment:
  python_version: "3.12"